spring.application.name=adventure-book

# Loads local secrets when .env exists
spring.config.import=optional:file:.env[.properties]
# JWT configuration. JWT_SIGNING_SECRET must be a Base64-encoded 256-bit (or longer) secret.
app.jwt.signing-secret=${JWT_SIGNING_SECRET}
app.jwt.access-token-expiry=${JWT_ACCESS_TOKEN_EXPIRY:PT1H}

# PostgreSQL connection
spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${POSTGRES_DB}
spring.datasource.username=${POSTGRES_USER}
spring.datasource.password=${POSTGRES_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Flyway database migrations
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# JPA / Hibernate
#spring.jpa.hibernate.ddl-auto=validate
spring.jpa.generate-ddl=false
spring.sql.init.mode=never
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true
